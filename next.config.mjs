/** @type {import('next').NextConfig} */
const nextConfig = {

    i18n: {
        locales: ["en", "ar", "ur"], // Supported languages
        defaultLocale: "en", // Default language
      },


};

export default nextConfig;
